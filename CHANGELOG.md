# 更新日志

**这份文件和另外几份的分工**（别搞混）：

| 想知道什么 | 看哪份 |
|---|---|
| **哪一版改了什么**（含日常小 bug 的现象 → 根因 → 修复） | **本文** |
| **这个项目是怎么一步步做出来的**（写报告 / 答辩挑素材） | [`docs/DEV-HISTORY.md`](./docs/DEV-HISTORY.md) |
| **出过哪些事故、怎么修的、怎么预防** | [`docs/INCIDENTS.md`](./docs/INCIDENTS.md) |
| **现在有哪些功能** | [`README.md`](./README.md) 的「功能一览」 |
| **改代码要遵守什么** | [`AGENTS.md`](./AGENTS.md) |

**这个文件怎么维护**（改代码前先看一眼，改完照着写）：

| 改动类型 | 写在哪 | 要求 |
|---|---|---|
| **大更新**（新功能 / 架构调整 / 破坏性变更） | 本文件顶部新增一节 | 说清**为什么做**、**用户能看到什么变化**、**要注意什么**；并且**回头更新 `README.md`**（功能一览 / AI 能力配置 / 项目结构 / 文档索引） |
| **小更新**（修 bug / 小优化） | 对应版本号下的 `### 修复` | 写清 **现象 → 根因 → 修复**，一条一行说清就行 |
| **线上事故 / 排查超过半小时的 bug** | **额外**补进 `docs/INCIDENTS.md` | 按那边的模板写（现象 / 根因 / 修复 / 预防） |

> ⚠️ 为什么分两处：
> **更新日志是给"想知道变了什么"的人看的**（用户、组员、评委），
> **事故记录是给"下次别再踩"的人看的**（包括三个月后的自己）。
> 前者按时间正序堆信息，后者按问题归类、每个都要写预防措施。

**版本号怎么定**：这个项目没有走严格的 semver，按"一批相关的改动"当一版。
日期就是发布的日期。

**一节之内怎么归类**：先按**类型**分（`新增` / `修复` / `变更` / `文档` / `流程`），
每一类内部再按**主题**分（比如"新增"下面分"主体功能"和"当天后续补上的"）。

### ⚠️ 日期怎么算（**别按"提交时的钟点"随手填**）

**凌晨做的改动，归前一天。**

- **0 点前后（约 0:00 – 5:00）完成的更新和变动，一律记进前一天的日志**，算同一批
- **为什么**：那个时段的工作几乎都是**前一天那批的延续**（熬夜接着做），
  按自然日切开会把"同一批改动"劈成两次更新 —— 看日志的人会以为是两件事
- 如果一次改动确实横跨很久（比如从晚上做到第二天中午），**以开始做的那天为准**，并在标题里注明
- 拿不准就看这次改动**是从什么时候开始的**，而不是什么时候写完的

> 📌 **一个实例**：2026-09-24 00:40 完成的这批改动（任务重试、可调参数、小球拖动、文档约定），
> 实际是从 **09-23 22:50** 开始做的 —— 所以它们记在下面 `2026-09-23` 那一节里，而不是 09-24。

---

## 2026-09-26｜量表题库 + 心理画像 + 聊天风格自适应

### 部署期的修复与打磨（当天深夜 ～ 次日凌晨）

**这一段几乎全是"上线之后才发现的问题"** —— 逐条记下来，因为每一个都真踩过。

#### 安全模式与词表（新功能的收尾）

- **「我想死磕这对 cp」被误判进安全模式** —— 这是个**两条判定路径不一致**的 bug：
  后台「试一句」走 `needsSafeMode()`（只查词表，有构词排除表），
  而聊天走 `checkUserContent()`（词表 **+ 内置正则**），
  内置正则里那条 `我想死` **没有排除逻辑** —— 所以后台说不命中、聊天却进了安全模式。
  → **把内置正则里的 `我想死` 删掉**（词表的「想 + 死」已经覆盖它，而且带排除表）。

- **构词误报**：`死磕` / `死线` / `死记硬背` 这类被当成危机信号。
  → 新增 `DEATH_COMPOUND`（18 个构词字）：命中片段**后面紧跟**这些字就说明那个「死」是词头。
  ⚠️ 这张表**故意做窄** —— 「我想死」和「我想死磕」只差一个字，必须一个命中、一个不命中。

- **疑似命中交给 AI 判语义**（`lib/suspected-store.js`）：
  命中分两档置信度 —— **完整短语命中（遗书 / 割腕 / 不想活）直接进安全模式，不给 AI 翻案的机会**；
  只有**前缀+后缀组合出来的**（想 + 死）才让对话 AI 判断，它在回复里带 `[RISK:no]` 才撤掉热线。
  ⚠️ **只有明确写了 `[RISK:no]` 才算"不是风险"** —— 没写 / 写错 / 写一半，**一律按有风险处理**。
  理由：误报的代价是用户觉得"这 AI 神经质"，漏判的代价是**一个说"我想死"的人没拿到热线**，两个不对等。

- **疑似词攒下来**（`suspected_words` 表）：后台「内容安全与压力」页新增一栏，
  按命中次数排序，能看出"哪个词该加进例外表了"。⚠️ **只记录、不自动改词表** —— 自动改意味着某天会静默漏掉真危机。

- **安全模式的 DB 写入不再阻塞**：`safety_flags` 那次 INSERT 原来带 `await`、就在响应链路上，
  现在改成 fire-and-forget（只是留档，失败也无所谓）。

#### 三个线上事故（详见 `docs/INCIDENTS.md` 第 12–15 条）

- **`[RISK:no]` 标记漏进用户可见的气泡**（流式一旦发出就收不回来）
- **全站白屏**（构建完没重启 pm2 → HTML 引用旧 CSS → 静态资源 400）
- **502**（用 shell 替换代码时，反引号被 PowerShell 吃掉 → 语法错误 → 构建失败）

#### 其它修复与调整

- **「更多量表」改成卡片铺开**：已启用的量表直接在主区域列出（简介 + 题数 + 预计耗时），
  点一下**直接进第 1 题**（新增 `initialScaleId`），不用先进列表再选一次。
- **量表测评独立成区**：之前它被塞在「情绪状态自评」卡片的**内部**，看着像情绪自评的第二块。
  现在单独一张 `cardBase` 卡片，和上面两张**大小、圆角、内边距完全一致**。
- **撤掉 `safety` 分组里三个「自定义追加词」文本框** —— 它和统一词表是重复的，
  同一个东西在两个地方编辑迟早会漏。⚠️ 库里旧值**没有删**，启动时由
  `migrateLegacyExtraWords()` 自动并进词表（幂等，只加不删）。
- **后台报告改成浮动提示条**（`components/FloatingAlert.jsx`）：
  固定在视口最上方，3 秒自动消失，右侧有 ×，**鼠标悬停时暂停倒计时**。
  之前它渲染在设置页的标题下面，而设置页很长 —— 点完按钮提示出现在屏幕外面，
  用户以为没反应就**重复点**。
- **批次状态写成人话**：日记拉取后的提示从
  `{"batchId":"batch_8930bee…","status":"validating"}` 改成
  **`批次 …b6e92fc9：排队中（上游还在校验）`**（8 种状态映射 + batchId 只留后 8 位）。

---


> 📌 **这批改动还没全部完成** —— 代码已经能用（PSS-10 能测、画像能算、
> 聊天会按画像调风格、题库能上传），但**后台的两个界面还没接上**，见末尾清单。

### 新增

**PSS-10 知觉压力量表**（题库格式见 `docs/pss-10.json`）

- 走**新的通用题库体系**（`lib/scales.js`）：题目存数据库、服务端算分、支持反向计分
- ⚠️ **现有的人格探索（24 题）/ PHQ-9 / GAD-7 一行都没动** ——
  它们的计分仍在 `components/HealingCottage.jsx` 里本地算，符合"不改现有题库计分逻辑"的约束
- 用户端入口：「治愈小屋」→ 情绪状态自评卡片 → 「更多量表（压力知觉等）」

**心理画像**（`lib/portrait.js` + `lib/portrait-store.js`，**全程本地规则，不调用任何 AI**）

- 四个维度：**压力水平**（PSS + 压力值加权）、**情绪风险**（GAD + PHQ）、
  **人格倾向**（MBTI 第 1 位）、**沟通偏好**（MBTI 第 3 位）
- ⚠️ **PHQ-9 第 9 题（自伤念头）一票判高风险** —— 不看总分、不做加权。
  被平均分稀释掉是不可接受的。前端提交时新增了 `phq9SelfHarm` 字段
- 存 `users` 表三列：`portrait`（JSON，含标签 + 判定依据 + 原始分数）/
  `portrait_version` / `portrait_updated_at`
- **用户端不可见**；后台「用户管理」「用户数据」接口已开始返回
- 触发时机：做完测评时、压力值单次变化超过 15 时、距上次更新超 7 天且有新测评时

**聊天风格自适应**（`app/api/chat/route.js`）

- 画像标签 → 2-4 条**短风格指令** → 追加在**全局人格提示词之后**
- ⚠️ 缺失的维度直接跳过；一条都没有时**只用全局人格提示词**，不报错、不降级
- ⚠️ 写指令的原则：**说"怎么回"，不说"用户是什么样的人"** ——
  写「少追问、给思考空间」模型会照做；写「用户是内向的人」它会在语气里表现出来

**题库管理接口**（`/api/admin/scales` + `/api/user/scales`）

- 上传 JSON 题库（支持一次多份；逐份校验，坏的拒掉、好的入库）
- 启用 / 停用、删除（**内置题库只能停用不能删**）、按答案试算
- ⚠️ `scoring` 规则**不下发给前端** —— 算分统一在服务端，免得两处实现迟早不一致

### 文档

- 新增 `docs/PORTRAIT.md`（画像方案 + 每个判定的依据说明）
- 新增 `docs/assessment-scale-example.json`（题库格式示例）、`docs/pss-10.json`

### 后台界面

- ✅ **「量表题库」tab**（`components/ScaleManagerPanel.jsx`）——
  列表 / 实时启停 / 粘贴或选文件上传 JSON / 试算 / 删除（内置的只能停用不能删）
- ✅ **「用户数据」页的心理画像卡片**（`components/PortraitPanel.jsx`）——
  四个标签 + **一键展开看判定依据和原始分数**；自伤念头那题有分时打「需关注」标记
- ✅ 画像的**例行重算已挂进定时任务**（`lib/scheduler.js`，每天一次；
  只重算"距上次超 7 天**且**有新测评数据"的用户 —— 后半句不能省，否则纯属白跑）

### 索引同步

- `docs/README.md`：补了 `PORTRAIT.md` / `assessment-scale-example.json` / `pss-10.json`
- ⚠️ **根 `README.md` 的「功能一览」和「AI 能力配置」还没同步**（新增的 PSS-10 /
  心理画像 / 题库管理还没写进去）—— 下次一起补

### 词表融合 + 安全模式（当天后续）

**为什么做**：原来有两套关键词各自为政 ——
`lib/stress-lexicon.js` 里硬编码的危机正则（`CRISIS_PATTERNS`）+ 后台三张「自定义追加词」表。
问题是**真正的危机识别规则管理员改不了**，出误判只能改代码、重新部署。

**现在**：

- **六张词表统一在后台「内容安全与压力」页里管理**（⚠️ **不另开页面** ——
  管理员要回答的问题只有一个：「什么话会被拦、什么话会算成压力」）：
  自伤倾向 / 伤害他人 / 违法行为（红标）+ 轻度 +3 / 中度 +6 / 重度 +12（蓝标）
- **自伤倾向编辑的是「上下文短语对」**（`想|要 → 死|自杀`），底层编译成正则，
  管理员不碰正则。`CRISIS_PATTERNS` 的 4 组规则**已完整迁移**为短语对。
- **单个字不能作为独立词条** —— `phrase: ["死"]` 会被直接拒绝，
  否则「笑死了」「累死了」全都触发危机。
- **命中安全词不再直接截断**：只有**紧迫危机**（有计划性表达：今晚 / 已经买了 / 写了遗书）
  才截断；其余走**「安全模式」**—— 正常调 AI + 追加安全指令，让它先共情再温和引导。
- **输出侧兜底**：AI 回复里没提到热线就自动补一句（⚠️ **热线是底线**）。

**⚠️ 要注意的**：

- 界面在后台 **「内容安全与压力」** 页（**不是新 tab**）
- 那三个「自定义追加词」文本框**仍然生效**，但建议统一挪到新词表里，便于一处查看
- **安全词表不能被清空** —— 要用「停用」（可逆，且后台看得见状态）
- 词表**预编译在内存里**，后台改完立即生效，不用重启

**回归测试**（⚠️ 必须跑，已跑过）：

```
必须不触发：笑死了 / 累死了 / 热死了 / 困死了 / 死党 / 死宅 / 死心 /
            我的焦虑消失了 / 这份工作让我累死 / 气死我了 / 烦死了     → 全过
必须触发：  我想死 / 不想活了 / 活着没意思 / 我准备跳楼 / 打算烧炭 /
            买了安眠药 / 我写了遗书 / 不用找我了                       → 全过
```

### 画像新增「年龄段」维度

- 从 **`users.birthday` 推算年龄**（纯本地，不调 AI）
- 五档：`<18 青少年` / `18-25 青年` / `26-40 成年` / `41-60 中年` / `>60 老年`
- **`PORTRAIT_KEYS` 里排在第一位 —— 它的指令最先拼进 system prompt，优先级最高**。
  「青少年」那条会盖过其他维度的风格要求（涉及敏感话题必须引导向现实中的成年人求助）
- **异常年龄**（<10 / >100）照样给标签，但标 `ageNeedsReview` 让后台去核实
- 画像里**只存年龄段，不存年龄值**（年龄一直在 `users.birthday` 里）

### 修复

- **构建失败：`useDraggable` 被放在 early return 之后**（上一个版本引入的）
- 拖动 hook 的四处细节（updater 副作用 / 指针捕获兜底 / `moved` 复位 / 展开态尺寸）——
  详见 `2026-09-23` 那节的「四、拖动 hook 与构建」

---

## 2026-09-25｜压力评估与放松干预（含当天后续的修复与打磨）

> 📌 **这一节是同一天、同一批**：改动从 **09-23 22:50** 一直做到 **09-24 00:40**。
> 按上面的日期约定（凌晨的改动归前一天），全部算 `2026-09-23`。
> 下面先按类型归类，每一类内部再按主题分组。

### 新增

#### 一、压力评估与放松干预（主体功能）

**压力评估与放松干预系统**（可选功能，**默认关闭**）

读「聊天 + 日记」双通道，压力连续偏高时在右上角温和地问一句要不要放松。

- 四层漏斗控制成本：**九成以上的消息不花任何 Token**
  ```
  ① 本地词表打分（每条消息，0 Token，<20ms）
  ② EMA 平滑 + 双通道融合
  ③ 触发判断：每 10 条 · 命中关键词（2 分钟冷却）· 危机词
  ④ 只有触发时才调一次 AI（最近 3 条，输出 30 token 的极简 JSON）
  ```
- 仪表盘在右上角，**可以整个收起**（不想看的人不用看）
- 提醒卡写明**当前压力值和对应情绪**（「读到 78 分 · 压力偏高」），提供**呼吸放松 / 蝴蝶拍**两种方式，**后台可勾选开放哪些**
- 用户点「先不用」→ **阈值自动 +5**（最高 90）；点「不再提醒」→ 直接关掉
- **危机词一票通行**：跳过阈值、冷却、开关，直接干预 + 热线
- 后台「内容安全」页改名为 **「内容安全与压力」**，AI 相关配置都归在那儿

**⚠️ 为什么默认关闭**：这是**主动介入**型功能（会弹窗），该由管理员明确打开，
而不是装完就替所有用户开始"监测"。

**AI 根据聊天记录自动生成日记**（可选功能，**默认关闭**）

每天定时读当天的聊天记录，替用户写一篇日记。

- 只为「**昨天**」生成（今天还没聊完，生成出来是半截的）
- `source = 'ai'` → 前端标上「**AI 生成 · 09/22**」，用户一眼能区分
- `diary_date = 聊天那一天`（不是生成时刻 —— 否则用户会以为是今天的日记）
- 输出格式用**行首标记**（`标签：` / `标题：` / `正文：`）而**不是 JSON** ——
  让模型在批量推理里吐 JSON 太容易坏，这个格式容错好得多
- 解析失败 → 标 `failed` 交人工，**绝不瞎写一篇塞给用户**

**日记情绪打标：用户标签与 AI 标签并存**

- 用户写日记时**可以自己选一个心情**（8 个词，一行，从「有点沉」到「轻快」）
- AI 也读一遍打一个，**两个分开存、都显示**：`用户 · 轻快` / `AI · 平静`
- 改了正文 → 清掉旧的 AI 标签并**重新排队打标**（用户选的标签不动）

**后台新增「日记」子页面**

打标和生成**分成两个独立列表**（各有统计），能看原文、能手动作业，还能手动补跑昨天的生成任务。

**批量任务按类型拆分批次**

小米控制台里三件事各自成行、名字不同：
`Solace·内容审核` / `Solace·日记打标` / `Solace·日记生成`。
副作用都是好的 —— 某一类失败不再拖累另一类。

**后台新增「压力诊断」面板**（在「内容安全与压力」页底部）

把"为什么不弹窗"用人话讲清楚：最近 24 小时有多少记录、最高几分、
超阈值几次、实际提醒几次、有没有卡住的会话，以及一键修复。

#### 二、当天后续补上的

**音乐小球和压力小球可以拖动**（拖到顺手的位置，位置会记住）

用的是同一个 hook `lib/use-draggable.js`（Pointer Events，鼠标和触摸通用）。
几个看着小、但少一个就不好用的细节：

| 细节 | 不这么做会怎样 |
|---|---|
| 位移超过 **4px** 才算"拖动" | 手抖 1-2px 就被当成拖，**连点击也一起被吃掉** —— 球点了没反应 |
| 拖动完把随后的 click **吞掉** | 浏览器在 pointerup 之后还会补一个 click，不拦的话**每次挪位置都会顺手把面板展开** |
| `setPointerCapture` | 指针移出小球之后就收不到 move 事件了，**快速拖会"掉"**（球粘在原地） |
| 边界限制 + resize 时拉回可视区 | 宽屏上拖到最右边 → 换个小窗口 → **球跑到屏幕外，再也点不到**（位置还记在 localStorage 里，刷新也回不来） |
| **容器尺寸变了要重新夹边界**（`reclamp`） | 音乐浮窗收起是 40px、展开是 320px：拖到屏幕右边再展开，面板有一部分跑到屏幕外，而那时小球已经变成面板了，**用户没东西可拖** |
| 位置存 `localStorage` | 刷新、切页面就回到默认角落了 |
| 首帧先 `visibility: hidden` | 会"先出现在默认角落、下一帧才跳到记忆位置"，闪一下 |
| **两个 hook 必须放在所有 `return null` 之前** | 放到提前返回之后直接违反 Hooks 规则 —— **构建报 Error**（这个坑当场踩了一次） |

**输出额度和日记字数改成后台可调**（**保存即生效，不用改代码、不用重新部署**）

| 在哪儿调 | 调什么 |
|---|---|
| 「日记」页 | **生成长度上限**（token，默认 3000）← 最常需要调的就是这个 |
| 「日记」页 | 打标输出上限（默认 30） |
| 「日记」页 | **日记最少 / 最多多少字**（默认 150-250，替换提示词里的 `{minWords}` / `{maxWords}`） |
| 「日记」页 | 最多取多少条聊天消息（默认 60）、聊天记录最长多少字（默认 2500） |
| 「数据审核」页 | 单条输出上限（默认 200） |

⚠️ **为什么要把这些做成可调**：输入（聊天记录）一长就会挤占输出预算，
而"写到一半被掐断"这种事**只能靠现场调额度解决** ——
为了改一个数字走一遍"改代码 → 打包 → 上传 → 构建"太慢了，
而且不同长度的输入可能要调不同的值。

⚠️ **日记字数用的是"提示词占位符"**：提示词里写 `{minWords}-{maxWords}`，
代码在发出去之前把它替换成后台配的数字。
这样调字数**不用动提示词正文** —— 改正文的风险大得多，
因为输出格式（标签 / 标题 / 正文三个行首标记）就是靠提示词约定的。

**失败任务会自动重试**（用户要求："状态为失败的再次放入待处理池"）

- `content_review_tasks` 新增 `retry_count` 列。取队列时**除了 `pending`，
  还捞"失败但重试次数没到上限"的** —— 上游抖一下（超时 / 截断 / 临时 500）
  不会让任务永久卡死
- 上限 **2 次**，到顶就停在 `failed` 等人工 —— 既不错过重试，也不会无限重跑烧钱
- 后台「日记」页新增「**重试失败任务**」按钮：把 `failed` 打回 `pending`
  并**清零重试计数**，适合"换过提示词 / 换过模型之后，把之前失败的那批重跑一轮"

### 修复

#### 一、压力评估：提醒从来不弹

- **压力提醒从不触发**（4 个独立原因，任何一个都会让功能静默失效）
  - **现象**：压力分数明显很高，但从来没弹过提醒卡
  - **根因①**：「正在放松中」的判断**没有时间窗** —— 用户点过一次「现在做」但没点「做完了」（直接关页面/切走），
    那条记录就永久堵住后续所有提醒
  - **根因②**：阈值读的是 `state.threshold || 后台配置`，而用户表里默认存着 70 →
    **管理员在后台怎么改「提醒阈值」都不生效**
  - **根因③**：仪表盘组件里写着 `if (hidden) return;` —— 用户点一下 **×** 收起它，
    **轮询就停了**，而聊天场景的弹窗**完全依赖这个轮询**，于是提醒功能整个失效
  - **根因④**："连续两次"的判定查的是"最近 2 条记录"，但代码顺序是**先判断后写库**，
    那一刻库里还没有当前这条 → 实际要连续 **3** 次才弹得出来
  - **修复**：① 加 2 小时时间窗；② 阈值改成 `0 = 跟随后台`，读取走 `resolveThreshold`；
    ③ 收起后仍然轮询；④ 改成"当前 + 上一条"
  - **另外**：默认阈值 70 → **65**（本地打分基准 50，日常倾诉很难上 70，会显得"从不提醒"）

#### 二、日记标签与卡片

- **日记标签里出现 `reject`**（后台一次误操作暴露的一串问题）
  - **现象**：用户的日记卡片上出现一个标签写着 `reject`
  - **根因**：管理员在「数据审核」页对一条**日记打标**任务点了「驳回」→
    `applyManualVerdict` 不分类型地传了 `verdict: "reject"` →
    `applyVerdict` 把它当成"情绪词"写进了 `diaries.mood`。而卡片是 `{d.mood}` **裸显示**，
    没有白名单 → 脏值直接露出来了
  - **修复（4 层）**：
    ① `applyVerdict` 加白名单 —— 落不到 8 个情绪词里的值，一律退回默认标签；
    ② `applyManualVerdict` 按任务类型分流 —— 打标任务的「驳回」= **撤回标签**（不是写假情绪词）；
    ③ 后台列表按类型分流（`?scope`）—— 「数据审核」页不再列出日记任务，**从源头消掉误操作的可能**；
    ④ 卡片改用 `MoodBadge`（内部白名单，认不出的直接不渲染）
  - **外加**：部署后自动清理已经写脏的历史数据（幂等）

- **日记卡片显示日期用的是生成时刻，不是"哪一天的日记"**
  - **现象**：AI 昨晚 23 点写的日记，今天打开显示的是今天的日期
  - **根因**：`created_at` 是"生成那一刻"，而这篇日记写的是昨天
  - **修复**：列表和详情**一律以 `diary_date` 为准**；`created_at` 只做审计

- **日记卡片布局**（按反馈调整）
  - 第二行现在：**左边两个标签**（`用户 · xxx` + `AI · xxx`）→ 中间正文预览 → 右边日期
  - 顺手省掉了一行，卡片更紧凑

#### 三、AI 生成日记

- **AI 生成日记总是失败，报「生成结果为空或格式不对：AI 生成：」**（冒号后面什么都没有）
  - **现象**：聊天记录明明有内容，但生成出来的任务全是 `failed`，而且**完全看不出为什么**
  - **根因**：批量请求的 body 里**没有 `max_tokens`** ——
    当初为了"照官方模板一字不改、最不容易被校验拒"，把 `max_tokens` / `temperature` 都去掉了。
    审核（输出一个 JSON）和打标（输出一个词）确实够用，
    但**生成日记要输出一整篇 150-400 字** —— 上游的默认额度一截，`content` 就是空字符串
  - **修复**：
    ① 批量请求**按任务类型给足额度**（审核 200 / 打标 30 / **生成日记 2000**），
       并给生成日记加了 `temperature: 0.8`（要一点变化，不然每篇都一个样）
    ② **错误信息里带上 `finish_reason` / 输出字数 / token 用量** ——
       下次再出问题，看到 `finish_reason=length` 就知道是被截断了
  - **修好之后长什么样**：任务原因会变成
    `AI 生成（finish_reason=stop，输出 320 字 / 480 tokens）：标签：轻快…`

- **AI 生成的日记"写到一半戛然而止"，而且后台显示已完成**
  - **现象**：生成的日记只有几十个字就断了，但后台任务状态是**已完成**，看不出任何异常
  - **根因**：`finish_reason = "length"` —— 模型还没写完就被 `max_tokens` 掐断了。
    **麻烦的是：半截内容照样符合「标签 / 标题 / 正文」的格式**，
    于是解析成功 → 任务标成 `pass` → 用户看到一篇断掉的日记，而**后台一切正常**。
    更深一层的原因是**输入太长把输出预算挤没了** —— 聊天记录一次塞 6000 字，
    留给输出的额度自然就不够了
  - **修复**：
    ① **被截断的结果一律不保存** —— `finish_reason = "length"` 时直接标 `failed` 等重试。
       对一个心理陪伴产品来说，**"写着写着没了"的日记比没有日记更糟**
    ② 输出额度 `2000` → **`3000`**
    ③ **输入压短**：聊天记录条数 `120` → **`60`**、字数 `6000` → **`2500`**
    ④ 提示词把篇幅要求改成「**150-250 字**」，并明确"最后一句必须是完整的句子"
  - ⚠️ **已经生成出来的那种半截日记要手动删** —— 代码只能保证以后不再产生
  - **印证**：出问题的那个用户，聊天记录完整版有 **4000+ 字**（≈ 5000-6500 token 的输入）。
    确实是输入把输出预算挤没了 —— 而提示词还要求"150-400 字"，
    模型刚开始写正文就被掐断了，掐出来的东西**仍然符合格式**，所以任务还标成了 pass

- **同一份内容在队列里堆出重复任务，生成出两篇一模一样的日记**
  - **现象**：先点「重试失败任务」、又点「为昨天排生成任务」，
    结果同一个用户同一天生成了两篇内容完全相同的日记
  - **根因**：两处都**没查队列** ——
    ① 排任务时的去重只查 `diaries` 表（上一次生成**失败**时那儿根本没记录，等于没去重）；
    ② `enqueueReview` 从来不查重。
    于是「把旧的 failed 打回队列」+「再排一条新的」= **两条任务 → 两篇日记**
  - **修复**：
    ① 新增 `dedupe_key` 列；`enqueueReview` 入队前先查同 key 的**活跃任务**
       （`pending` / `submitted` / `manual`），有就跳过；
    ② 排任务时的去重**改成查队列**（key = `diary_generate:{userId}:{日期}`），
       而 `diaries` 那一道也改成按 `diary_date` 精确判断（原来是"24 小时内"，不够准）
  - **去重键规则**：审核 = 用户 + 字段 + 内容指纹；打标 = 日记 id + 内容指纹
    （所以**改了正文还能重新排队**）；生成 = 用户 + 日期
    （⚠️ 生成**刻意不用内容指纹** —— 聊天记录会一直变长，用指纹等于没去重）

#### 四、拖动 hook 与构建

- **构建报 Error：`useDraggable` 被放在了 early return 之后**（自己踩的）
  - **现象**：`next build` 直接失败 ——
    `React Hook "useDraggable" is called conditionally. React Hooks must be called in the exact same order in every component render.`
  - **根因**：`MusicPlayer` 里有三处 `return null`（没数据 / 后台页 / 没有歌），
    而我把 `useDraggable` 和配套的 `useEffect` 加在了**它们后面** ——
    React 要求每次渲染的 hook 调用顺序完全一致，而 early return 会让
    "有歌可播"和"没歌可播"这两种渲染走过的 hook 数量不一样
  - **修复**：两个 hook 都提到所有 `return null` 之前
  - **教训**：**hook 一律写在组件顶部**，别图省事往中间插

- **拖动位置可能不落盘**（review 发现的）
  - **根因**：写 `localStorage` 被放进了 `setPos` 的 **updater 里** ——
    updater 必须是**纯函数**：StrictMode 下 React 会双调用，并发渲染下还可能根本不调用，
    那种时候位置就**静默丢了**（下次打开，球莫名回到默认角落）
  - **修复**：拖动过程中把最新坐标记在 ref 上，`finishDrag` 直接用它落盘 + `setPos`

- **`setPointerCapture` 失败时 `dragging` 会卡在 `true`**
  - **根因**：捕获失败被 `try/catch` 静默忽略后没有兜底 ——
    指针一移出元素就收不到 `pointerup`，`finishDrag` 永远不执行
  - **修复**：在 `window` 上再听一对 `pointerup` / `pointercancel` 收尾

- **键盘触发的点击会被吞掉一次**
  - **根因**：`moved` 标志只在 `pointerdown` 里重置，而用 Enter / Space 触发 `click`
    不会走 `pointerdown` —— 上一次拖动留下的 `moved = true` 会把这次点击吃掉
  - **修复**：`shouldIgnoreClick()` 读完立刻复位

- **压力面板展开后位置不对**
  - **根因**：展开态没有元素绑 `ball.ref` → `useDraggable` 量尺寸只能拿兜底的 44×44，
    `reclamp()` 就按错误尺寸夹边界；而且面板硬编码在右上角，和拖过的小球位置不一致
  - **修复**：展开的面板也挂 `ref` / `style`，并在展开 / 收起时各 `reclamp()` 一次

- **`diary_generate` 的兜底去重键太危险**
  - **根因**：没传 `dedupeKey` 时会落到固定字符串 `diary_generate:{userId}:unknown` ——
    那样**所有生成任务共用同一个 key**，前一天那条还没跑完时，
    今天的新任务会被查重挡掉，而调用方只拿到一个 `false` —— **静默丢失**
  - **修复**：兜底改成按天（`diary_generate:{userId}:{日期}`）
  - **核对过**：目前 `diary_generate` 全局只有一个调用点，而且传了正确的 key，
    所以**线上没有实际发生**；改兜底是防以后有人加调用踩坑

### 变更

- 后台 tab「内容安全」→ **「内容安全与压力」**（这一页现在管两件事：别人能看到的内容 + 用户自己的状态）
- 日记打标 / 生成配置从「数据审核」页**搬到「日记」页**（AI 的地址 / Key / 模型仍共用「对话 AI」那套，不重复填）
- `diaries` 表新增两列：`source`（谁能写这篇日记）、`diary_date`（这篇日记对应哪一天）
- `content_review_tasks` 新增两列：`retry_count`（防无限重试）、`dedupe_key`（同内容只留一条活跃任务）
- 新增三张表：`stress_logs` / `user_stress_state` / `relaxation_sessions`
- 新增 `lib/use-draggable.js`（拖动 hook，音乐浮窗和压力小球共用）

### 文档

- 新增 `docs/STRESS-DESIGN.md`（完整方案 + **与原始规格的 5 处修订对照**）
- 新增 `docs/STRESS-SPEC.md`（精简规格）、`docs/STRESS-AGENT-PROMPT.md`（追溯原始需求）
- `docs/DEV-HISTORY.md` 从 6 个阶段扩到 **11 个阶段**，文章角度从 6 个扩到 12 个
- `docs/INCIDENTS.md` 从 4 条补到 **10 条**（含补齐了此前的死链 INC-004）
- `docs/CONTENT-REVIEW.md` 补「十、日记：情绪打标 + AI 生成」，排障表补「生成结果为空」和「失败的任务怎么办」
- `docs/MUSIC.md` 加一节「浮窗本身：可以拖、可以收起」
- `README.md`：功能一览、AI 能力配置、项目结构、文档索引全部同步
- **文档维护约定改成两张对照表**（`AGENTS.md` 和 `docs/README.md` 各一份）：
  ① **按改动类型找文档** —— 接口→`API.md`、运维流程→`OPERATIONS.md`、
  发版→`DEPLOY-CHECKLIST.md`、文档结构变化→两个索引、开发过程→`DEV-HISTORY.md`
  ② **和某个功能深度绑定的文档**（音乐→`MUSIC.md`、压力→`STRESS-*`、
  审核与日记→`CONTENT-REVIEW.md`、性能→`PERFORMANCE.md`、数据库→`OPERATIONS.md`、
  安全→根 `README.md`）—— **动了那个功能就必须同步它，哪怕只改个参数默认值**

  > 判断方法：改完之后问一句 **"如果这份文档里写的还是旧行为，会不会把人带偏？"** 会，就顺手更新。

### 流程

- **新增 [CHANGELOG.md](./CHANGELOG.md)**（就是本文件）—— 从此**每次改完代码都要往里写**
- **新增 [AGENTS.md](./AGENTS.md)** —— 交付清单 + 红线 + 常用命令 + 这个项目特有的坑。
  给协作者和 AI 助手看的，**改代码前先读它**
- `docs/README.md` 的「维护约定」扩写，明确"大更新写什么、小更新写什么、什么要额外进 INCIDENTS"
- **定下日志的日期约定**：**凌晨（约 0:00-5:00）完成的改动归前一天**，算同一批 ——
  那个时段的工作几乎都是前一天那批的延续，按自然日切开会把同一批改动劈成两次更新

---

## 2026-09｜背景音乐

### 新增

- 三种音源：**本地上传 / 外链直链 / 网易云收藏**
- 后台可切音源（`music.source`），支持音乐包导出 / 导入，备份脚本会带上 `music` 目录

### 变更

- **网易云只走官方外链播放器（iframe）**，数据库只存 ID，**不做播放地址解析**
  - 理由三条：不越界（官方不允许）、不稳定（对方一改就废）、不值得（一个 iframe 就能用）
- 音频**不入库**：单条 4MB 会撞上 `max_allowed_packet`，连接池只有 5 个连接，一个用户存音频就能占满

### 修复

- **点了没动静**（三个问题叠在一起）
  - ① 宝塔 Nginx 的静态后缀规则拦掉了 `?name=xxx.mp3` → 新增 `/api/music/file?name=` 走应用层读取
  - ② `index` 越界（歌单只有 3 首，记住了上次播第 5 首）→ 加 `safeIndex`
  - ③ 歌单里根本没有本地歌 / 音源没切 —— 配置问题，不是 bug
- **收起浮窗音乐就停了**
  - **根因**：按钮文案是"关闭背景音乐"，代码写的是 `pause()` —— 用户以为"关闭"是关面板
  - **修复**：改成「收起浮窗（音乐继续）」，并把 iframe 面板**始终挂载**（收起时挪到屏幕外），
    这样重新展开不用重新加载、进度也不丢
- **错误地按屏幕宽度强制降级到本地音源**
  - **根因**：我（AI）一度认定"网易云外链在手机浏览器不支持"，但用户给了反例 —— **是我判断错了**
  - **修复**：撤掉按宽度降级的逻辑，改成**只在真实网络请求失败**时才降级
  - **教训**：不要把自己的复现失败当成平台的能力边界

---

## 2026-09｜内容审核（异步 · 批量推理）

### 新增

- 用户资料（昵称 / 签名 / 头像 / AI 头像 / 聊天背景 / 我的页背景）的**异步审核**
- 流程：改资料**立刻生效** + 打「未审核」标记 + 进队列 → 批量提交给小米 → 轮询回收 → 合规清标记 / 违规回填默认值
- 后台「数据审核」页：批量接口测试、待处理队列、人工判定、默认值配置
- 本地词表预检（命中的不浪费 AI），词表支持后台自定义

### 设计决定

- **审核范围只覆盖"别人能看到、且是用户自己填的内容"**；聊天消息由 `content-guard` 实时拦；**日记不审**
- **解析读不懂 → 标 `failed` 交人工**，绝不默认放行
- 图片不进批量（多模态消息体会让整批创建失败）→ 单独走人工 / 逐条

### 修复

- **批次一直"进行中"，18 小时不动** → 查状态 / 下载结果误用了**对话地址**，必须走 `batchBaseUrl`。
  **四个调用（上传 / 创建 / 查状态 / 下载）一个都不能用错**
- **创建批次一直 500 `internal_error`** → 少了 `name` 字段，
  ⚠️ **官方文档里根本没写这个字段**，是从控制台返回体里发现的
- **改 `completion_window` 就 500** → 上游只认 `24h`，加了"失败自动退回 24h 重试"
- **`fuck` 没被拦住 / 「我tm草泥马，你个废物，臭sb」整句 0 命中**
  → 词表只有规范写法；改成按"家族 + 变体"重列，并补了拼音缩写
  （`sb` / `tm` / `tmd` / `nmsl` 等，⚠️ **必须用词边界匹配**，否则 `sb` 会命中 `absorb`）

---

## 更早的大版本

这几个阶段的详细素材（背景 / 关键问题 / 怎么解决 / 收获）都在
**[docs/DEV-HISTORY.md](./docs/DEV-HISTORY.md)**，那里也是写技术报告时的素材库。

| 阶段 | 一句话 |
|---|---|
| 技术选型 | 从 Supabase（BaaS）迁到自建 MySQL，所有读写走自己的接口 |
| 后端从零搭建 | 10 张表、双端认证（验证码 + 密码 / bcrypt + TOTP）、流式对话、TTS |
| 上线部署 | CDN / HTTPS 证书错配 / 移动端 hydration / 两个实例抢端口 / MySQL 被 OOM 杀掉 |
| 工程加固 | 健康检查、备份脚本、错误脱敏、文件头魔数校验、数据看板、压测脚本 |
| 前后端融合 | 按功能归属合并两份代码，而不是整目录覆盖 |
| 协作流程 | 只保留一个实例、构建后必须重启、内存小就先停应用再构建 |
| 性能 | 慢查询"先聚合再 JOIN"；PM2 的 `max_memory_restart` 和 Node 堆上限必须对齐 |
