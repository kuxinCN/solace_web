# 项目约定

> 这份文件是给**协作者和 AI 助手**看的：动手之前先读「红线」，改完代码照着「交付清单」走。
> 项目是什么、有哪些功能 → 看 [README.md](./README.md)；
> 文档地图 → 看 [docs/README.md](./docs/README.md)。

---

## 一、改完代码必须做的三件事

### 1. 更新 `CHANGELOG.md`（**每次都要**）

| 改动类型 | 写在哪 | 要求 |
|---|---|---|
| **大更新**（新功能 / 架构调整 / 破坏性变更） | 顶部新增一节 | 说清**为什么做**、**用户能看到什么变化**、**要注意什么** |
| **小更新**（修 bug / 小优化） | 对应版本的 `### 修复` | 写清 **现象 → 根因 → 修复** |
| **线上事故 / 排查超过半小时的 bug** | **额外**补 `docs/INCIDENTS.md` | 按那边的模板（现象 / 根因 / 修复 / 预防） |

#### ⚠️ 日期怎么算（**别按"提交时的钟点"随手填**）

**凌晨做的改动，归前一天。**

- **0 点前后（约 0:00 – 5:00）完成的更新和变动，一律记进前一天的日志**，算同一批
- **为什么**：那个时段的工作几乎都是**前一天那批的延续**（熬夜接着做），
  按自然日切开会把"同一批改动"劈成两次更新 —— 看日志的人会以为是两件事
- 如果一次改动确实横跨很久（比如从晚上做到第二天中午），**以开始做的那天为准**，并在标题里注明
- 拿不准就看这次改动**是从什么时候开始的**，而不是什么时候写完的

### 2. 大更新**还要**回头更新 `README.md`

至少同步这几处：**功能一览**（用户端 / 管理后台）、**AI 能力配置**、**项目结构**、**文档索引**。

> ⚠️ 这一步最容易漏 —— 出现过"代码全做完了，README 里连功能名都搜不到"的情况。

### 3. 按改动类型更新对应文档（**主动对照，别等人提醒**）

| 改了什么 | 更新哪份 |
|---|---|
| **接口**（新增 / 改字段 / 改状态码） | `docs/API.md` |
| **运维流程**（备份 / 日志 / 监控 / 发布步骤 / 端口 / 定时任务） | `docs/OPERATIONS.md` |
| **发版相关**（文件清单 / 上传步骤 / 回滚） | `docs/DEPLOY-CHECKLIST.md` |
| **文档结构**（新增 / 改名 / 删除文档） | `docs/README.md` 的索引 **+** 根 `README.md` 的文档索引 |
| **开发过程**（新的阶段 / 新的踩坑 / 可写的角度） | `docs/DEV-HISTORY.md` |
| **内容审核 / 日记打标 / AI 生成日记** | `docs/CONTENT-REVIEW.md` |
| **压力评估** | `docs/STRESS-DESIGN.md` + `docs/STRESS-SPEC.md` |
| **性能相关的改动** | `docs/PERFORMANCE.md` |

### 4. ⚠️ 和某个功能**深度绑定**的文档，动了那个功能就必须同步它

有些文档是**某个功能专属**的 —— 只要碰了那个功能就得改它，
**哪怕改动看起来很小**（改个参数默认值、调个阈值、换个措辞都算）：

| 动了什么功能 | 必须同步的文档 |
|---|---|
| **背景音乐 / 网易云 / 音源 / 播放器** | `docs/MUSIC.md` |
| **压力评估 / 放松提醒 / 危机干预** | `docs/STRESS-DESIGN.md` + `docs/STRESS-SPEC.md` |
| **内容审核 / 日记打标 / AI 生成日记** | `docs/CONTENT-REVIEW.md` |
| **性能 / 索引 / PM2 / 缓存 / 慢查询** | `docs/PERFORMANCE.md` |
| **数据库结构 / 备份 / 恢复 / 巡检** | `docs/OPERATIONS.md` |
| **登录 / 会话 / 限流 / 上传校验 / 密码加密** | 根 `README.md` 的「安全设计」那一节 |
| **接口**（任何前后端约定的变化） | `docs/API.md` |

> ⚠️ **为什么单独强调这一类**：**功能专属文档最容易过期** ——
> 它不在"每次都会翻"的路径上，平时没人看，等真要查的时候
> 才发现里面写的还是三个版本前的行为，比没有文档更误导人。
>
> **一个判断方法**：改完之后问自己一句 ——
> **"如果这份文档里写的还是旧行为，会不会把人带偏？"** 会，就顺手更新。

---

## 二、红线（改之前先读，这些都是踩过坑才定下来的）

### 架构与部署

| 红线 | 为什么 |
|---|---|
| ⛔ **不要用 `instrumentation.js`** | Next 14 会为 server 和 edge **各编译一次**，webpack 顺着依赖链解析到 `node:path`，edge 没有 → **整个 build 失败**。需要在启动时跑的东西走「懒启动」（挂在 `/api/health` 上） |
| ⛔ **批量推理的四个调用必须全走 `batchBaseUrl`** | 上传 / 创建 / 查状态 / 下载。少一个或用错地址 → 批次会永远显示"进行中" |
| ⛔ **创建批次必须带 `name` 字段** | ⚠️ **官方文档里没写，但不传直接 500** |
| ⛔ **不要同时跑两个实例** | pm2 和宝塔 Node 项目二选一，否则出现"改了代码一半生效" |
| ⛔ **构建后必须重启** | `npm run build` 只是生成 `.next`，跑着的进程还加载着旧代码 |
| ⚠️ 服务器上 `cp` 有 `alias cp='cp -i'` | 覆盖文件用 `\cp -rf`，否则会一直等你确认 |

### 产品与交互

| 红线 | 为什么 |
|---|---|
| ⛔ **日记不做内容审核** | 日记是私人空间，不是"别人能看到的内容"。审核范围只覆盖昵称 / 签名 / 头像 / 背景 / 聊天消息 |
| ⛔ **情绪标签不做数值评分、不做趋势对比、不用红黄绿** | 产品是"陪伴"，不是"给用户的情绪打分"。8 个温和词就够 |
| ⛔ **压力评估不诊断** | 界面上出现的是"情绪档位"（「有点累」），不是"你有焦虑倾向"。档位措辞后台可配 |
| ⛔ **压力评估不给后台看个体数据** | 这数据比聊天记录还敏感。后台只给聚合口径 |
| ⛔ **AI 生成的内容必须明确标注** | 用户得知道不是自己写的，否则会以为账号被盗 |
| ⛔ **网易云只走官方外链播放器** | 不做播放地址解析：不越界（官方不允许）、不稳定（对方一改就废）、不值得（iframe 够用） |

### AI 与文本处理

| 红线 | 为什么 |
|---|---|
| ⛔ **审核类系统的默认分支必须是"不确定"** | 读不懂格式 → 标 `failed` 交人工。**绝不能默认"通过"** |
| ⛔ **危机词用带上下文的正则，不用 `includes()`** | 中文没有词边界：`includes("消失")` 会把「焦虑**消失**了」判成危机（那是好转）；`includes("死")` 会命中「笑死我了」 |
| ⛔ **提示词里不要给具体的情绪示例** | 模型对示例的模仿远强于对规则的遵守 —— 你写"比如你提到今天很累"，它就真的会对开心的日记说"我读到你很累" |
| ⚠️ **中文内容匹配注意词边界** | `sb` 会命中 `absorb`、`tm` 会命中 `atm`；拼音缩写必须用词边界 |
| ⚠️ **让模型输出结构化结果时，优先"行首标记"而不是 JSON** | 批量推理里 JSON 太容易坏。格式：`标签：xxx` / `标题：xxx` / `正文：`（后面全是正文） |

---

## 三、这个项目特有的坑

| 坑 | 说明 |
|---|---|
| **配置存在数据库里** | 改 `lib/settings.js` 的默认值**不会影响已存在的配置**（比如线上提示词）。改完要在后台**手动也改一遍** |
| **老库升级靠 `ensureUserColumnsOnce()`** | 新加字段 / 新表**要注册进去**，否则老部署上不会自动补。它内部每步独立兜底（`safe()`），一步失败不影响其它步骤 |
| **加新配置分组要三处同步** | `GROUPS` 数组、`DEFAULTS`、以及后台 `FIELDS`。漏了 `GROUPS` 会报"未知的配置分组" |
| **`mysql2` 的 DATE 列出来是 JS Date 对象** | 直接 JSON 出去是带时区的 ISO 串，前端切字符串会切错（严重时差一天）。要手动裁成 `YYYY-MM-DD` |
| **PM2 的两个内存参数必须对齐** | `max_memory_restart` 和 `--max-old-space-size` 只设一个 = 定时炸弹（表现是安静地反复重启，日志里没报错） |
| **构建吃内存** | 小内存服务器：先 `pm2 stop` 再 build，并加 swap |
| **浏览器缓存** | `Failed to find Server Action "x"` 这类报错通常是浏览器拿着旧页面，无害；用无痕或 Ctrl+F5 |

---

## 四、常用命令

### 本地打包（Windows / PowerShell）

```powershell
# 打包成部署用的 zip（排除 node_modules / .next / 配置）
# 已验证多次，产物默认放 E:\ai web project\solace-deploy.zip
robocopy <源码目录> <临时目录> /E /XD "<源码目录>\node_modules" "<源码目录>\.next" "<源码目录>\.vercel" "<源码目录>\.git" "<源码目录>\.reasonix" "<源码目录>\logs" "<源码目录>\backups" "<源码目录>\config" /XF .env.local db.json installed.lock
Add-Type -AssemblyName System.IO.Compression.FileSystem
[System.IO.Compression.ZipFile]::CreateFromDirectory(<临时目录>, <zip 路径>)
```

### 服务器部署（CentOS）

```bash
cd /tmp && rm -rf sp && mkdir sp && unzip -o /tmp/solace-deploy.zip -d sp
\cp -rf /tmp/sp/. /www/wwwroot/solace/
cd /www/wwwroot/solace && pm2 stop solace && rm -rf .next && npm run build && pm2 delete solace && pm2 start ecosystem.config.js
```

**⚠️ 两个都不能省**：
- `rm -rf .next` —— 不删会出现 `Could not find a production build`
- `\cp -rf` —— 服务器上 `cp` 有 `-i` 别名

### 排障

```bash
pm2 logs solace --lines 100          # 看应用日志
curl -s http://127.0.0.1:3000/api/health   # 应用是否活着（含数据库延迟）
systemctl status mysqld              # 数据库是否活着
```

---

## 五、关键路径速查

| 项 | 值 |
|---|---|
| 应用端口 | **3000**（写死在 `ecosystem.config.js`） |
| 服务器项目路径 | `/www/wwwroot/solace` |
| Node | 宝塔装的 v24.21.0（`/www/server/nodejs/v24.21.0/bin`） |
| 定时任务 | `/api/health` 或后台审核页的第一个请求会**懒启动** `lib/scheduler.js` |
| 老库自动升级 | 访问用户日记页 / 后台任意页会触发 `ensureUserColumnsOnce()` |

---

## 六、写作与文档约定

- **中文优先**，但**代码、标识符、路径、命令保持原文**（不要翻译）
- 文档里**用大白话解释"为什么"**，别只写"做了什么" —— 三个月后看得懂的是原因，不是动作
- 注释里凡是**「⚠️」开头**的，都表示"这里是踩过坑才这样的，别改"
- 更新日志写「现象 → 根因 → 修复」；事故记录额外写「预防措施」
