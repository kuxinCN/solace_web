# Solace

> 给每一个不想说话的灵魂，一点慰藉。

一个**情绪陪伴**应用：用户可以找人说话、写日记、用情绪急救工具缓一缓。

**产品边界（很重要）**：
- 它不是心理咨询、不是治疗，**不做任何诊断**；
- AI 的角色是**倾听和陪伴** —— 不讲道理、不给人生建议、不评判对错、不催人振作；
- 界面和交互上刻意回避"打分""评级""进步/退步"这类表达（详见「日记情绪标签」一节）。

---

## 目录

- [功能一览](#功能一览)
- [技术栈](#技术栈)
- [快速开始（本地开发）](#快速开始本地开发)
- [首次部署](#首次部署)
- [管理后台](#管理后台)
- [AI 能力配置](#ai-能力配置)
- [安全设计](#安全设计)
- [运维](#运维)
- [项目结构](#项目结构)
- [文档索引](#文档索引)
- [常见问题](#常见问题)

---

## 功能一览

### 用户端

| 功能 | 说明 |
|---|---|
| **三种登录方式** | 邮箱验证码（自动注册）、账号密码、找回密码（邮箱验证码重置） |
| **和 AI 倾诉** | 流式回复、逐字打字机效果；AI 人设由后台配置，可随时调整 |
| **语音朗读** | 把 AI 的回复读出来（TTS），支持 OpenAI 兼容协议与小米 MiMo 协议 |
| **日记** | 新建 / 编辑 / 删除，附 AI 情绪标签；支持全文搜索 |
| **情绪急救箱** | 情绪来袭时的即时缓解工具 |
| **疗愈小屋** | 放松用的小空间 |
| **个人资料** | 昵称、头像、AI 头像、聊天背景；换图时支持**裁剪**（1:1 / 16:9） |
| **对话管理** | 新建、改名、置顶、删除、按内容搜索 |
| **开屏动画** | 进入应用时的欢迎层 |

### 管理后台

| 模块 | 说明 |
|---|---|
| **数据看板** | 用户 / 对话 / 消息 / 日记总量与今日新增、趋势柱状图、情绪分布、活跃用户 Top5（邮箱掩码） |
| **用户管理** | 查看全部用户、手动添加/编辑/删除、启用或禁用、查看或清空密码；字段显示开关（账号/密码/邮箱强制显示） |
| **用户数据** | 查看某个用户的日记、对话、消息明细，可逐条删除 |
| **对话 AI** | 接口地址、Key、模型、温度、**系统提示词（可随时改，保存即生效）**、读日记时的附加提示词 |
| **语音 TTS** | 接口类型（OpenAI 兼容 / 小米 MiMo）、Key、模型、音色、格式、朗读风格指令、试听 |
| **邮箱 / 验证码** | SMTP 配置、测试发信、验证码位数/有效期/冷却/每日上限、邮件主题与正文模板 |
| **数据库** | 查看当前连接、修改并**热切换**（先测连通性，失败不改动线上配置） |
| **站点信息** | 站点名称、后台公告 |
| **操作日志** | 全部敏感操作的审计记录 |

---

## 技术栈

| 层 | 选型 |
|---|---|
| 框架 | **Next.js 14.2**（App Router）+ React 18 + Tailwind CSS 3 |
| 数据库 | **MySQL 5.7**（utf8mb4），10 张表，全部走预处理语句 |
| 进程管理 | **PM2**（`ecosystem.config.js`） |
| 反向代理 | **Nginx**（HTTPS + gzip + 强制跳转） |
| 对话 AI | **OpenAI 兼容协议**（`/chat/completions`），默认智谱 GLM |
| 语音合成 | **OpenAI 兼容**（`/audio/speech`）或**小米 MiMo**（`/chat/completions` + base64） |
| 邮件 | **nodemailer** + 网易 163/126 SMTP |
| 认证 | 用户端会话 Cookie；后台 **TOTP 动态验证码** + bcrypt |
| 图片存储 | 压缩为 base64 直接存库（不落磁盘，部署更简单） |

---

## 快速开始（本地开发）

```bash
npm install
npm run dev          # http://localhost:3000
```

环境变量：复制 `.env.example` 为 `.env.local` 后按需填写。

**数据库**：

- 方式一（推荐）：访问 `http://localhost:3000/install`，在向导里填数据库连接，**自动建好全部 10 张表**
- 方式二：手动导入 `db/schema.sql`，然后访问 `/admin` 走后台初始化流程

---

## 首次部署

服务器部署（CentOS 7 / 8.5 + 宝塔 + MySQL 5.7）的完整步骤见 **[DEPLOY.md](./DEPLOY.md)**，
包含 Node 版本、建库、上传代码、PM2 启动、Nginx 反代 + HTTPS、安装向导、验收清单与常见报错。

**部署后请务必做这几件事**（都在 [docs/OPERATIONS.md](./docs/OPERATIONS.md) 里有详细步骤）：

1. 配置数据库自动备份（`scripts/backup-db.sh` + 定时任务）
2. 配置 pm2 日志轮转（`pm2 install pm2-logrotate`）
3. 接一个外部可用性监控，探针填 `https://你的域名/api/health`
4. 确认只有一个应用实例在跑，且 MySQL 与应用都配了开机自启

**安装入口的处理**：安装完成后会写安装锁 `config/installed.lock`，此后 `/install` 与 `/api/install` 一律拒绝再次安装。
确实要重装需在 `.env.local` 设 `ALLOW_REINSTALL=1` + `REINSTALL_KEY=口令`（需服务器权限）。
**重装只重置管理员账号，用户、聊天、日记等业务数据不受影响。**

---

## 管理后台

入口：**`/admin`**

- 登录方式：**账号 + 密码 + 6 位动态验证码**（TOTP，微软/Google 验证器扫码绑定）
- 连续 5 次失败会锁定 10 分钟。若被锁，在数据库执行：
  ```sql
  UPDATE admin_users SET failed_attempts = 0, locked_until = NULL;
  ```
- 忘记动态验证码：重新生成密钥并写入 `admin_users.totp_secret`，再用验证器重新绑定即可（详见 OPERATIONS.md）

---

## AI 能力配置

### 对话 AI

后台「对话 AI」页面，填**接口地址 + API Key + 模型名**即可，必须兼容 **OpenAI 协议**：

| 服务商 | 接口地址示例 |
|---|---|
| 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` |
| DeepSeek | `https://api.deepseek.com/v1` |
| OpenAI | `https://api.openai.com/v1` |
| 小米 MiMo | `https://api.xiaomimimo.com/v1` |

> 接口地址**填到版本目录、或直接粘官方文档给的完整地址（`…/v1/chat/completions`）都可以**，程序会自动识别，不会重复拼接。

### 语音 TTS

后台「语音 TTS」页面先选**接口类型**，两种协议的请求方式不同：

| 接口类型 | 端点 | 认证头 | 音频返回位置 |
|---|---|---|---|
| **OpenAI 兼容** | `{接口地址}/audio/speech` | `Authorization: Bearer` | 响应体（二进制） |
| **小米 MiMo** | `{接口地址}/chat/completions` | `api-key` | `choices[0].message.audio.data`（base64） |

小米 MiMo 参考配置：地址 `https://api.xiaomimimo.com/v1`、模型 `mimo-v2.5-tts`、音色 `mimo_default`、格式 `mp3`。
（注意 Key 前缀要与地址对应：`sk-` 配 `api.xiaomimimo.com`，`tp-` 配 `token-plan-cn.xiaomimimo.com`）

### 提示词（AI 人设）

**提示词存在数据库里，后台改完保存立即生效，不用重启也不用重新构建。**

- 「AI 提示词（人设）」：角色设定与说话风格。留空则回退到前端自带的提示词
- 「读日记时的附加提示词」：用户写完日记后追加给 AI 的指令，用 `{diary}` 表示日记正文位置

> 这是答辩/演示很好用的功能：现场改一句提示词，用户端回复风格立刻变化。

### 日记情绪标签

AI 会给每篇日记打一个情绪标签，用于让用户感到"被看见"。**它只有 8 个温和的描述词**：

```
轻快 · 平静 · 安稳 · 有点沉 · 疲惫 · 烦躁 · 孤单 · 说不清
```

**设计约定（改动前请先读）**：

| 约定 | 原因 |
|---|---|
| 用词表白名单，模型从 8 个词里选 | 防止 AI 写出"抑郁倾向明显"这类诊断性词汇 |
| **不做数值评分** | 没有"62 分"，不给情绪排名 |
| **不做趋势对比** | 不出现"你比上周更糟了" |
| 标签只呈现，不触发自动建议 | 不弹"要不要做个深呼吸练习" |
| 拿不准就输出「说不清」 | 允许说不清，不逼着给答案 |

接口：`POST /api/user/diaries/mood`（详见 [docs/API.md](./docs/API.md)）

---

## 安全设计

| 项 | 做法 |
|---|---|
| **会话** | 数据库只存 `sha256(token)`，即使被拖库也无法直接登录；Cookie 为 HttpOnly |
| **管理员密码** | bcrypt 哈希，不可逆 |
| **管理员登录** | 密码 + TOTP 动态验证码双因素；连续 5 次失败锁定 10 分钟 |
| **用户密码** | AES-256-GCM 可逆加密（业务要求后台可查看，安全权衡见 DEPLOY.md 附录） |
| **SQL 注入** | 全部使用预处理语句；动态列名走白名单映射 |
| **XSS** | 邮件模板与日志输出统一做 HTML 转义；前端不用 `dangerouslySetInnerHTML` |
| **限流** | 发码、校验码、登录、上传、改密、改邮箱、AI 调用、安装入口均有独立限流 |
| **错误信息** | 默认脱敏，MySQL 原始报错只写服务端日志，不返回给前端 |
| **上传校验** | kind 白名单 → data URL 前缀 → **文件头魔数比对真实格式** → 格式一致性 |
| **审计** | 登录、改配置、查看明文密码等敏感操作全部写 `audit_logs` |
| **备份脚本** | 数据库密码走临时配置文件，不出现在进程列表里 |

---

## 运维

| 能力 | 位置 |
|---|---|
| **健康检查** | `GET /api/health` —— 返回应用状态、数据库版本、运行时长、延迟；异常返回 503。不含任何敏感信息，可接外部监控 |
| **数据库备份** | `scripts/backup-db.sh` —— 自动读 `config/db.json`，gzip 导出并清理 7 天前的旧备份 |
| **日志轮转** | `pm2 install pm2-logrotate`（防止日志撑爆磁盘） |
| **压测** | `scripts/bench.sh` —— 输出 QPS / P50 / P95 / P99 |
| **慢查询/巡检** | 见 [docs/OPERATIONS.md](./docs/OPERATIONS.md) 的巡检清单与故障速查表 |

---

## 项目结构

```
app/
  page.js                    用户端登录页（验证码 / 账号密码 / 找回密码 + 开屏动画）
  chat/page.js               聊天主界面（会话、消息、日记、疗愈小屋、急救箱、TTS）
  install/page.js            安装向导（环境自检 → 数据库 → 管理员 + TOTP 绑定）
  admin/page.js              管理后台（看板、用户、AI、TTS、邮箱、数据库、站点、日志）
  api/
    health/                  健康检查
    chat/  tts/              对话与语音合成
    install/                 安装向导
    auth/                    发送验证码 / 校验登录 / 账号密码登录 / 找回密码 / 会话 / 退出
    user/                    资料、上传、邮箱、密码、统计、会话、消息、日记、情绪标签、标题生成
    admin/                   后台全部接口（初始化、会话、配置、数据库、用户、用户数据、看板、日志、测试）
components/                  用户端组件（疗愈小屋、急救箱、个人资料、图片裁剪、开屏动画等）
lib/
  db.js                      MySQL 连接池（支持后台热切换）
  schema.js                  建表与自动补列（老部署升级时自动补新字段）
  settings.js                分组配置读写与校验（配置存数据库，改完即生效）
  ai.js                      对话 / 语音 / 情绪标签 / 标题生成
  mailer.js                  SMTP 发信与邮箱验证码
  user-auth.js               用户端会话（替代 Supabase Auth）
  admin-auth.js              后台认证（bcrypt + TOTP + 会话 + 锁定 + 审计）
  secret-box.js              用户密码的可逆加密（AES-256-GCM）
  rate-limit.js              进程内限流
  install.js / util.js       安装状态与通用工具
db/schema.sql                完整表结构（手工建库用）
scripts/
  backup-db.sh               数据库备份
  bench.sh                   简易压测
docs/
  API.md                     接口文档（全部 45 个端点）
  OPERATIONS.md              运维手册（备份、日志、巡检、故障速查）
  DEPLOY-CHECKLIST.md        部署清单与前端对接说明
ecosystem.config.js          PM2 启动配置
DEPLOY.md                    详细部署教程
```

---

## 文档索引

| 文档 | 内容 |
|---|---|
| [DEPLOY.md](./DEPLOY.md) | 从零部署：CentOS + 宝塔 + MySQL + Nginx + HTTPS |
| [docs/API.md](./docs/API.md) | 全部接口说明、认证方式、状态码、限流一览、表结构 |
| [docs/OPERATIONS.md](./docs/OPERATIONS.md) | 备份与恢复、日志轮转、日常巡检、故障速查、改代码标准流程 |
| [docs/DEPLOY-CHECKLIST.md](./docs/DEPLOY-CHECKLIST.md) | 更新部署清单、回滚方法、前端对接说明 |

---

## 常见问题

**Q：页面报 `Internal Server Error`（整页只有这一句话）？**
A：多半是 `.next` 构建产物不完整。执行：

```bash
cd /项目目录
pm2 stop solace
rm -rf .next
npm run build        # 必须看到 ✓ Compiled successfully
pm2 start solace
```

**Q：域名 502 / 500，但 `127.0.0.1:3000` 正常？**
A：检查是否有**两个实例**在跑（宝塔 Node 项目 + pm2），以及 Nginx 的 `proxy_pass` 端口是否指向正在跑的那个：

```bash
ss -lntp | grep -E ':3000|:3001'
grep proxy_pass /www/server/panel/vhost/nginx/*.conf
```

**Q：构建报 `Build failed because of webpack errors`？**
A：真正原因在上面几行，跑这个直接看：

```bash
npm run build 2>&1 | grep -A4 "Can't resolve"
```

**Q：所有请求都卡住然后 500？**
A：先看数据库活着没：`systemctl status mysqld`，再 `curl -s http://127.0.0.1:3000/api/health`。

**Q：后台登录提示"账号已锁定"？**
A：连续错 5 次会锁 10 分钟，清掉即可：

```sql
UPDATE admin_users SET failed_attempts = 0, locked_until = NULL;
```

**Q：改完代码页面没变化？**
A：`npm run build` 之后**必须重启进程**（`pm2 restart solace`）；浏览器侧用无痕窗口或 `Ctrl + F5`。

**Q：TTS 试听报 400 / Invalid request？**
A：检查「接口类型」是否选对（OpenAI 兼容 vs 小米 MiMo 的请求方式完全不同），以及模型名、音色名是否拼对。

---

## 许可

本项目为课程/竞赛作品，仅供学习交流使用。
